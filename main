
# -*- coding: utf-8 -*-

from pathlib import Path
from dotenv import load_dotenv

from App.Kernel import main

path_env = Path('.') / '.env'
load_dotenv(dotenv_path=path_env)


if __name__ == '__main__':

    exit(main())
